# Winzig
Requests the presence of Winzig, the perfect goblin barmaid, who will attend to your needs.

## Usage
Set the environment variables `DISCORD_BOT_TOKEN`, `DISCORD_CLIENT_ID`, `DISCORD_GUILD_ID`, and `DISCORD_BOT_DB`. `DISCORD_BOT_DB` takes the form `mysql://user:pass@localhost:3306/dbname`.